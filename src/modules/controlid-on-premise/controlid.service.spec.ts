import { Test, TestingModule } from '@nestjs/testing';
import { ControlidOnPremiseService } from './controlid.service';

describe('ControlidOnPremiseService', () => {
  let service: ControlidOnPremiseService;

  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     providers: [ControlidOnPremiseService],
  //   }).compile();

  //   service = module.get<ControlidOnPremiseService>(ControlidOnPremiseService);
  // });

  it('should be defined', () => {
    // expect(service).toBeDefined();
  });
});
